document.addEventListener('DOMContentLoaded', function () {
  const contactsTbody = document.getElementById('contacts-tbody');

  // Load contacts from API (if you create an endpoint)
  // For now, this is a placeholder
  contactsTbody.innerHTML = '<tr><td colspan="5">Chưa có liên hệ nào hoặc cần tạo API endpoint.</td></tr>';
});

